import { Inter, Roboto, Anton, Courgette } from 'next/font/google'

export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
})

export const roboto = Roboto({
    weight: ['900'],
    style: ['normal'],
    subsets: ['latin'],
    display: 'swap',
})

export const anton = Anton({
    weight: ['400'],
    style: ['normal'],
    subsets: ['latin'],
    display: 'swap',
})

export const courgette = Courgette({
    weight: ['400'],
    style: ['normal'],
    subsets: ['latin'],
    display: 'swap'
})
